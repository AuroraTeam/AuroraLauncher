/**
 * enum for NodeJS.Platform
 */
export enum Platform {
    WINDOWS = 'win32',
    LINUX = 'linux',
    MACOS = 'darwin',
}

/**
 * enum for NodeJS.Architecture
 */
export enum Architecture {
    ARM = 'arm',
    ARM64 = 'arm64',
    X32 = 'ia32',
    X64 = 'x64',
}
